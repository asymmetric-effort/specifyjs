# @specifyjs/math

Lightweight, immutable linear algebra library for SpecifyJS. Provides 2D/3D
vector operations, NxN matrix math, and numerical solvers (LU decomposition,
Gaussian elimination, determinant, inverse).

All functions are pure, use `Float64Array` for numeric precision, and have zero
runtime dependencies.
